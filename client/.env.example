# Frontend environment variables (copy to .env.local)

# Optional: override API base URL (default in dev is /api via Vite proxy)
# VITE_API_BASE_URL=/api

# Server URL for the Vite dev proxy (must match PORT in server/.env)
VITE_API_PROXY_TARGET=http://localhost:5000
